using System.ComponentModel;

namespace GVisionWpf.Types
{
    public enum EEdgeDetectDirection
    {
        [Description("In → Out")]
        InToOut,
        [Description("Out → In")]
        OutToIn,
    }
}